# 8.4 云服务配置管理

> AWS、阿里云、腾讯云的配置文件，让 Claude Code 帮你管理和排查。

## 🎯 场景描述

云服务的配置管理是另一类"文档多但记不住"的工作：

- Terraform 配置语法复杂
- Serverless Framework 的 YAML 经常写错
- 云函数的权限配置让人头大
- 跨云迁移更是一场噩梦

**Claude Code 能帮你生成、审查和排错各种云配置。**

---

## ✨ 案例一：Serverless Framework 配置

### 需求

```
帮我写一个 serverless.yml：
- AWS Lambda + Node.js 20
- API Gateway HTTP API
- 3 个接口：POST /users、GET /users/:id、PUT /users/:id
- 连接 DynamoDB
- 环境变量从 SSM Parameter Store 读取
- 开发环境用 DynamoDB Local
```

### Claude Code 生成

```yaml
# serverless.yml
service: user-service

frameworkVersion: "3"

provider:
  name: aws
  runtime: nodejs20.x
  region: ap-southeast-1
  stage: ${opt:stage, 'dev'}
  environment:
    DYNAMODB_TABLE: ${self:service}-${self:provider.stage}
    JWT_SECRET: ${ssm:/user-service/${self:provider.stage}/jwt-secret~true}
  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - dynamodb:Query
            - dynamodb:PutItem
            - dynamodb:GetItem
            - dynamodb:UpdateItem
            - dynamodb:DeleteItem
          Resource: "arn:aws:dynamodb:${self:provider.region}:*:table/${self:service}-${self:provider.stage}"

functions:
  createUser:
    handler: src/handlers/createUser.handler
    events:
      - httpApi:
          path: /users
          method: post

  getUser:
    handler: src/handlers/getUser.handler
    events:
      - httpApi:
          path: /users/{id}
          method: get

  updateUser:
    handler: src/handlers/updateUser.handler
    events:
      - httpApi:
          path: /users/{id}
          method: put

resources:
  Resources:
    UsersTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: ${self:service}-${self:provider.stage}
        BillingMode: PAY_PER_REQUEST
        AttributeDefinitions:
          - AttributeName: userId
            AttributeType: S
        KeySchema:
          - AttributeName: userId
            KeyType: HASH

plugins:
  - serverless-esbuild
  - serverless-offline
```

---

## ✨ 案例二：Terraform 基础设施即代码

### 需求

```
帮我写 Terraform 配置：
- VPC（10.0.0.0/16）
- 2 个子网（公有和私有）
- ECS Fargate 集群
- RDS PostgreSQL
- ALB 负载均衡
- 安全组规则
```

### Claude Code 生成

```hcl
# main.tf
provider "aws" {
  region = var.aws_region
}

# ---- VPC ----
resource "aws_vpc" "main" {
  cidr_block           = "10.0.0.0/16"
  enable_dns_hostnames = true
  enable_dns_support   = true

  tags = { Name = "${var.project}-vpc" }
}

# ---- 子网 ----
resource "aws_subnet" "public" {
  count                   = 2
  vpc_id                  = aws_vpc.main.id
  cidr_block              = "10.0.${count.index}.0/24"
  availability_zone       = data.aws_availability_zones.available.names[count.index]
  map_public_ip_on_launch = true

  tags = { Name = "${var.project}-public-${count.index}" }
}

resource "aws_subnet" "private" {
  count             = 2
  vpc_id            = aws_vpc.main.id
  cidr_block        = "10.0.${count.index + 100}.0/24"
  availability_zone = data.aws_availability_zones.available.names[count.index]

  tags = { Name = "${var.project}-private-${count.index}" }
}

# ---- RDS ----
resource "aws_db_subnet_group" "main" {
  name       = "${var.project}-db-subnet"
  subnet_ids = aws_subnet.private[*].id
}

resource "aws_security_group" "db" {
  name   = "${var.project}-db-sg"
  vpc_id = aws_vpc.main.id

  ingress {
    from_port       = 5432
    to_port         = 5432
    protocol        = "tcp"
    security_groups = [aws_security_group.app.id]
  }

  egress {
    from_port   = 0
    to_port     = 0
    protocol    = "-1"
    cidr_blocks = ["0.0.0.0/0"]
  }
}

resource "aws_db_instance" "main" {
  identifier           = "${var.project}-db"
  engine               = "postgres"
  engine_version       = "16"
  instance_class       = "db.t3.micro"
  allocated_storage    = 20
  db_name              = var.db_name
  username             = var.db_user
  password             = var.db_password
  db_subnet_group_name = aws_db_subnet_group.main.name
  vpc_security_group_ids = [aws_security_group.db.id]
  skip_final_snapshot  = true

  tags = { Name = "${var.project}-rds" }
}
```

### Terraform 常用技巧

```
帮我给这个 Terraform 配置加上：
1. outputs 输出 RDS 端点地址
2. 变量文件分离（terraform.tfvars）
3. 远程状态管理（S3 + DynamoDB）
```

---

## ✨ 案例三：云函数（腾讯云 SCF）

### 需求

```
帮我写一个腾讯云云函数（Python）：
- 定时触发，每天凌晨 2 点执行
- 从 MySQL 查询前一天的数据
- 生成 CSV 报告
- 上传到 COS 对象存储
- 发送企业微信通知
```

### Claude Code 生成

```python
# main.py — 腾讯云云函数
import os
import csv
import json
import pymysql
from datetime import datetime, timedelta
from qcloud_cos import CosConfig, CosS3Client
import requests

def main_handler(event, context):
    """每日数据报告生成"""
    # ---- 查询昨天数据 ----
    yesterday = (datetime.now() - timedelta(days=1)).strftime('%Y-%m-%d')
    
    conn = pymysql.connect(
        host=os.environ['DB_HOST'],
        port=int(os.environ['DB_PORT']),
        user=os.environ['DB_USER'],
        password=os.environ['DB_PASS'],
        database=os.environ['DB_NAME'],
        cursorclass=pymysql.cursors.DictCursor
    )
    
    try:
        with conn.cursor() as cursor:
            cursor.execute("""
                SELECT id, username, action, created_at 
                FROM user_actions 
                WHERE DATE(created_at) = %s 
                ORDER BY created_at DESC
            """, (yesterday,))
            rows = cursor.fetchall()
    finally:
        conn.close()
    
    # ---- 生成 CSV ----
    filename = f"report_{yesterday}.csv"
    filepath = f"/tmp/{filename}"
    
    with open(filepath, 'w', newline='', encoding='utf-8-sig') as f:
        writer = csv.DictWriter(f, fieldnames=['id', 'username', 'action', 'created_at'])
        writer.writeheader()
        writer.writerows(rows)
    
    # ---- 上传到 COS ----
    config = CosConfig(
        Region=os.environ['COS_REGION'],
        SecretId=os.environ['COS_SECRET_ID'],
        SecretKey=os.environ['COS_SECRET_KEY']
    )
    client = CosS3Client(config)
    client.upload_file(
        Bucket=os.environ['COS_BUCKET'],
        Key=f"reports/{filename}",
        LocalFilePath=filepath
    )
    
    # ---- 发送通知 ----
    webhook_url = os.environ['WECHAT_WEBHOOK']
    requests.post(webhook_url, json={
        "msgtype": "markdown",
        "markdown": {
            "content": f"## 📊 每日数据报告\n"
                       f"**日期**: {yesterday}\n"
                       f"**记录数**: {len(rows)}\n"
                       f"[下载报告](https://{os.environ['COS_BUCKET']}.cos.{os.environ['COS_REGION']}.myqcloud.com/reports/{filename})"
        }
    })
    
    return {"statusCode": 200, "body": json.dumps({"records": len(rows), "file": filename})}
```

---

## 🔧 进阶技巧

### 1. 云配置排错

```
我的 CloudFormation 部署失败了，这是错误日志：
[粘贴日志]
帮我分析原因并修复模板
```

### 2. 成本优化

```
分析一下这个 AWS 账号的 Terraform 配置，
帮我找出可以降本的地方：
- 是否有未使用的资源
- 实例规格是否过大
- 能不能利用 Reserved Instance 或 Spot
```

### 3. 跨云迁移

```
把这个阿里云的配置迁移到 AWS：
- ECS → ECS Fargate
- RDS MySQL → RDS PostgreSQL
- OSS → S3
帮我写对应的 Terraform 配置
```

---

## ⚠️ 注意事项

1. **密钥用环境变量，绝不要写进代码** — Claude Code 生成的代码也会提醒你
2. **先用 terraform plan 看变更** — 别直接 apply，看清楚要改什么
3. **云函数冷启动有延迟** — Claude Code 可以帮你加预热策略
4. **云服务花钱如流水** — 建好预算告警，防止半夜被账单吓醒

## 🎓 学到了什么

1. **IaC 是现代运维的基础** — Claude Code 能帮你快速生成 Terraform/CloudFormation
2. **Serverless 不是万能的，但轻量任务特别合适** — 定时报告、数据处理、Webhook 处理
3. **跨云配置有规律可循** — 大部分云服务概念是对应的，Claude Code 知道怎么映射
4. **配置即代码的真正价值是可复现** — 比手动控制台点来点去靠谱一万倍
