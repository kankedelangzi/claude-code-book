# 2.2 API 接口编写

> 用 Claude Code 10 分钟完成一个生产级 RESTful API 接口，包含参数校验、错误处理和单元测试。

## 案例背景

假设你刚接手一个 Node.js 项目，产品经理要求你新增一个用户注册接口。传统开发流程是：

1. 写路由定义
2. 写参数校验逻辑
3. 写数据库操作
4. 写错误处理
5. 写单元测试
6. 手动测试

整个流程至少需要 1-2 小时。用 Claude Code，可以在 10 分钟内完成，并且代码质量更高。

## 前置条件

- 已安装 Claude Code CLI（`claude` 命令可用）
- 项目目录下有基本的 Node.js 项目结构
- 已有数据库连接配置（本例使用 Prisma ORM）

---

## 实战步骤

### Step 1：描述需求，让 Claude 规划

```bash
cd your-project
claude
```

在 Claude Code 中输入：

```text
我需要新增一个用户注册接口：
- 路由：POST /api/auth/register
- 参数：username（必填，3-20字符）、email（必填，邮箱格式）、password（必填，8-32字符）
- 功能：校验参数、检查邮箱是否已注册、创建用户记录、返回用户信息和 token
- 技术栈：Express + Prisma + JWT
- 要求：包含完整的错误处理和参数校验

请先规划实现方案，不要直接写代码。
```

Claude 会先分析项目结构，然后给出实现方案：

```
我来分析项目结构，规划用户注册接口的实现方案...

项目分析：
- 使用 Express 框架
- Prisma ORM 连接 PostgreSQL
- 已有 JWT 工具函数在 src/utils/jwt.ts
- 已有错误处理中间件在 src/middleware/errorHandler.ts

实现方案：
1. 创建 src/validators/authValidator.ts - 参数校验规则
2. 创建 src/services/authService.ts - 业务逻辑层
3. 创建 src/controllers/authController.ts - 控制器
4. 在 src/routes/auth.ts 中添加注册路由
5. 创建 src/__tests__/auth.test.ts - 单元测试

预计修改 5 个文件，新增约 200 行代码。是否开始实施？
```

确认后，Claude 会开始编写代码。

---

### Step 2：Claude 自动生成代码

Claude 会依次创建/修改以下文件：

#### 2.1 参数校验器 `src/validators/authValidator.ts`

```typescript
import { body, ValidationResult, validationResult } from 'express-validator';
import { Request, Response, NextFunction } from 'express';

export const registerValidation = [
  body('username')
    .trim()
    .isLength({ min: 3, max: 20 })
    .withMessage('用户名长度必须在 3-20 字符之间')
    .matches(/^[a-zA-Z0-9_]+$/)
    .withMessage('用户名只能包含字母、数字和下划线'),
  
  body('email')
    .trim()
    .isEmail()
    .withMessage('请输入有效的邮箱地址')
    .normalizeEmail(),
  
  body('password')
    .isLength({ min: 8, max: 32 })
    .withMessage('密码长度必须在 8-32 字符之间')
    .matches(/^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)/)
    .withMessage('密码必须包含大小写字母和数字'),
];

export const validate = (req: Request, res: Response, next: NextFunction) => {
  const errors = validationResult(req);
  if (!errors.isEmpty()) {
    return res.status(400).json({
      success: false,
      errors: errors.array().map(err => ({
        field: err.param,
        message: err.msg,
      })),
    });
  }
  next();
};
```

#### 2.2 业务逻辑层 `src/services/authService.ts`

```typescript
import { PrismaClient } from '@prisma/client';
import bcrypt from 'bcryptjs';
import { generateToken } from '../utils/jwt';
import { AppError } from '../middleware/errorHandler';

const prisma = new PrismaClient();

export interface RegisterInput {
  username: string;
  email: string;
  password: string;
}

export interface RegisterResult {
  id: string;
  username: string;
  email: string;
  token: string;
}

export class AuthService {
  /**
   * 用户注册
   */
  async register(input: RegisterInput): Promise<RegisterResult> {
    const { username, email, password } = input;

    // 检查邮箱是否已注册
    const existingUser = await prisma.user.findUnique({
      where: { email },
    });

    if (existingUser) {
      throw new AppError(409, '该邮箱已被注册');
    }

    // 检查用户名是否已存在
    const existingUsername = await prisma.user.findUnique({
      where: { username },
    });

    if (existingUsername) {
      throw new AppError(409, '用户名已被使用');
    }

    // 加密密码
    const hashedPassword = await bcrypt.hash(password, 10);

    // 创建用户
    const user = await prisma.user.create({
      data: {
        username,
        email,
        password: hashedPassword,
      },
    });

    // 生成 JWT
    const token = generateToken({ userId: user.id });

    return {
      id: user.id,
      username: user.username,
      email: user.email,
      token,
    };
  }
}

export default new AuthService();
```

#### 2.3 控制器 `src/controllers/authController.ts`

```typescript
import { Request, Response, NextFunction } from 'express';
import authService, { RegisterInput } from '../services/authService';

export class AuthController {
  /**
   * 用户注册
   * POST /api/auth/register
   */
  async register(req: Request, res: Response, next: NextFunction) {
    try {
      const input: RegisterInput = {
        username: req.body.username,
        email: req.body.email,
        password: req.body.password,
      };

      const result = await authService.register(input);

      res.status(201).json({
        success: true,
        data: result,
        message: '注册成功',
      });
    } catch (error) {
      next(error);
    }
  }
}

export default new AuthController();
```

#### 2.4 路由配置 `src/routes/auth.ts`

```typescript
import { Router } from 'express';
import authController from '../controllers/authController';
import { registerValidation, validate } from '../validators/authValidator';

const router = Router();

/**
 * @route POST /api/auth/register
 * @desc 用户注册
 * @access Public
 */
router.post(
  '/register',
  registerValidation,
  validate,
  authController.register.bind(authController)
);

export default router;
```

#### 2.5 单元测试 `src/__tests__/auth.test.ts`

```typescript
import request from 'supertest';
import app from '../app';
import { PrismaClient } from '@prisma/client