# 2.2 API 接口编写

## 场景描述

后端工程师的日常：写 API。

但手写 CRUD 很枯燥，而且容易漏掉错误处理、参数校验、文档等细节。

**Claude Code 可以帮你 5 分钟生成一个生产级的 RESTful API。**

---

## 案例：博客系统 API

我们要为一个博客系统编写完整的 RESTful API，包括：
- 文章的 CRUD（创建、读取、更新、删除）
- 分类管理
- 评论功能
- 用户认证（JWT）
- 参数校验和错误处理
- API 文档（Swagger）

---

## 第一步：生成 API 骨架

**提示词：**

```
在当前目录创建一个博客系统 API：
- 技术栈：Node.js + Express + SQLite
- 功能：文章 CRUD、分类管理、评论功能
- 必须包含：参数校验、错误处理、JWT 认证
- 生成 Swagger 文档
- 代码要生产级，包含完整的错误处理和参数校验
- 所有文件放在 ./blog-api/ 目录
```

**Claude Code 会生成：**

```
blog-api/
├── server.js              # 入口文件
├── config/
│   └── database.js        # 数据库配置
├── routes/
│   ├── posts.js           # 文章路由
│   ├── categories.js      # 分类路由
│   └── comments.js        # 评论路由
├── middleware/
│   ├── auth.js            # JWT 认证中间件
│   └── errorHandler.js    # 全局错误处理
├── models/
│   ├── Post.js            # 文章模型
│   ├── Category.js        # 分类模型
│   └── Comment.js         # 评论模型
├── swagger.json           # API 文档
├── package.json
└── README.md
```

---

## 第二步：检查生成的代码

**提示词：**

```
检查一下生成的 API 代码，确保：
1. 所有路由都有参数校验
2. 错误处理完整
3. JWT 认证正确实现
4. Swagger 文档可访问
```

Claude Code 会：
1. 审查代码质量
2. 指出潜在问题
3. 自动修复 bug

---

## 核心代码解析

### server.js（入口文件）

```javascript
const express = require('express');
const swaggerUi = require('swagger-ui-express');
const swaggerDoc = require('./swagger.json');
const { initDatabase } = require('./config/database');
const authMiddleware = require('./middleware/auth');

const app = express();
const PORT = process.env.PORT || 3000;

// 中间件
app.use(express.json());
app.use('/api-docs', swaggerUi.serve, swaggerUi.setup(swaggerDoc));

// 路由
const postsRouter = require('./routes/posts');
const categoriesRouter = require('./routes/categories');
const commentsRouter = require('./routes/comments');
const authRouter = require('./routes/auth');

app.use('/api/auth', authRouter);
app.use('/api/posts', authMiddleware, postsRouter);
app.use('/api/categories', authMiddleware, categoriesRouter);
app.use('/api/comments', authMiddleware, commentsRouter);

// 全局错误处理
const errorHandler = require('./middleware/errorHandler');
app.use(errorHandler);

// 启动服务器
initDatabase().then(() => {
  app.listen(PORT, () => {
    console.log(`✅ Server running on http://localhost:${PORT}`);
    console.log(`📚 API Docs: http://localhost:${PORT}/api-docs`);
  });
});
```

---

### routes/posts.js（文章路由）

```javascript
const express = require('express');
const router = express.Router();
const Post = require('../models/Post');
const { validatePost } = require('../middleware/validators');

// 获取所有文章（支持分页和筛选）
router.get('/', async (req, res, next) => {
  try {
    const { page = 1, limit = 10, category, status } = req.query;
    const offset = (page - 1) * limit;
    
    const posts = await Post.findAll({
      limit: parseInt(limit),
      offset: parseInt(offset),
      category,
      status
    });
    
    const total = await Post.count({ category, status });
    
    res.json({
      success: true,
      data: posts,
      pagination: {
        page: parseInt(page),
        limit: parseInt(limit),
        total,
        totalPages: Math.ceil(total / limit)
      }
    });
  } catch (error) {
    next(error);
  }
});

// 获取单篇文章
router.get('/:id', async (req, res, next) => {
  try {
    const post = await Post.findById(req.params.id);
    if (!post) {
      return res.status(404).json({
        success: false,
        error: 'Post not found'
      });
    }
    res.json({ success: true, data: post });
  } catch (error) {
    next(error);
  }
});

// 创建文章
router.post('/', validatePost, async (req, res, next) => {
  try {
    const { title, content, category_id, status = 'draft' } = req.body;
    const author_id = req.user.userId; // 从 JWT 中获取
    
    const post = await Post.create({
      title,
      content,
      author_id,
      category_id,
      status
    });
    
    res.status(201).json({ success: true, data: post });
  } catch (error) {
    next(error);
  }
});

// 更新文章
router.put('/:id', validatePost, async (req, res, next) => {
  try {
    const { title, content, category_id, status } = req.body;
    const post = await Post.findById(req.params.id);
    
    if (!post) {
      return res.status(404).json({
        success: false,
        error: 'Post not found'
      });
    }
    
    // 检查权限（只能编辑自己的文章）
    if (post.author_id !== req.user.userId) {
      return res.status(403).json({
        success: false,
        error: 'Forbidden'
      });
    }
    
    const updated = await Post.update(req.params.id, {
      title,
      content,
      category_id,
      status
    });
    
    res.json({ success: true, data: updated });
  } catch (error) {
    next(error);
  }
});

// 删除文章
router.delete('/:id', async (req, res, next) => {
  try {
    const post = await Post.findById(req.params.id);
    
    if (!post) {
      return res.status(404).json({
        success: false,
        error: 'Post not found'
      });
    }
    
    // 检查权限
    if (post.author_id !== req.user.userId) {
      return res.status(403).json({
        success: false,
        error: 'Forbidden'
      });
    }
    
    await Post.delete(req.params.id);
    res.json({ success: true, message: 'Post deleted' });
  } catch (error) {
    next(error);
  }
});

module.exports = router;
```

---

### middleware/validators.js（参数校验）

```javascript
const { body, validationResult } = require('express-validator');

// 文章参数校验
exports.validatePost = [
  body('title')
    .notEmpty()
    .withMessage('Title is required')
    .isLength({ min: 3, max: 200 })
    .withMessage('Title must be 3-200 characters'),
  
  body('content')
    .notEmpty()
    .withMessage('Content is required')
    .isLength({ min: 10 })
    .withMessage('Content must be at least 10 characters'),
  
  body('category_id')
    .optional()
    .isInt()
    .withMessage('Category ID must be an integer'),
  
  body('status')
    .optional()
    .isIn(['draft', 'published'])
    .withMessage('Status must be draft or published'),
  
  (req, res, next) => {
    const errors = validationResult(req);
    if (!errors.isEmpty()) {
      return res.status(400).json({
        success: false,
        errors: errors.array()
      });
    }
    next();
  }
];
```

---

### middleware/auth.js（JWT 认证）

```javascript
const jwt = require('jsonwebtoken');
const config = require('../config');

module.exports = (req, res, next) => {
  try {
    // 从 Authorization header 中获取 token
    const authHeader = req.headers['authorization'];
    const token = authHeader && authHeader.split(' ')[1]; // Bearer TOKEN
    
    if (!token) {
      return res.status(401).json({
        success: false,
        error: 'Access token required'
      });
    }
    
    // 验证 token
    jwt.verify(token, config.JWT_SECRET, (err, user) => {
      if (err) {
        return res.status(403).json({
          success: false,
          error: 'Invalid or expired token'
        });
      }
      req.user = user;
      next();
    });
  } catch (error) {
    next(error);
  }
};
```

---

### models/Post.js（文章模型）

```javascript
const db = require('../config/database');

class Post {
  static async findAll({ limit, offset, category, status }) {
    let query = 'SELECT * FROM posts WHERE 1=1';
    const params = [];
    
    if (category) {
      query += ' AND category_id = ?';
      params.push(category);
    }
    
    if (status) {
      query += ' AND status = ?';
      params.push(status);
    }
    
    query += ' ORDER BY created_at DESC LIMIT ? OFFSET ?';
    params.push(limit, offset);
    
    return new Promise((resolve, reject) => {
      db.all(query, params, (err, rows) => {
        if (err) reject(err);
        else resolve(rows);
      });
    });
  }
  
  static async findById(id) {
    return new Promise((resolve, reject) => {
      db.get('SELECT * FROM posts WHERE id = ?', [id], (err, row) => {
        if (err) reject(err);
        else resolve(row);
      });
    });
  }
  
  static async create({ title, content, author_id, category_id, status }) {
    return new Promise((resolve, reject) => {
      db.run(
        `INSERT INTO posts (title, content, author_id, category_id, status)
         VALUES (?, ?, ?, ?, ?)`,
        [title, content, author_id, category_id, status],
        function (err) {
          if (err) reject(err);
          else resolve({ id: this.lastID, title, content, status });
        }
      );
    });
  }
  
  static async update(id, { title, content, category_id, status }) {
    return new Promise((resolve, reject) => {
      db.run(
        `UPDATE posts SET title = ?, content = ?, category_id = ?, status = ?
         WHERE id = ?`,
        [title, content, category_id, status, id],
        function (err) {
          if (err) reject(err);
          else resolve({ id, title, content, status });
        }
      );
    });
  }
  
  static async delete(id) {
    return new Promise((resolve, reject) => {
      db.run('DELETE FROM posts WHERE id = ?', [id], (err) => {
        if (err) reject(err);
        else resolve(true);
      });
    });
  }
  
  static async count({ category, status }) {
    let query = 'SELECT COUNT(*) as total FROM posts WHERE 1=1';
    const params = [];
    
    if (category) {
      query += ' AND category_id = ?';
      params.push(category);
    }
    
    if (status) {
      query += ' AND status = ?';
      params.push(status);
    }
    
    return new Promise((resolve, reject) => {
      db.get(query, params, (err, row) => {
        if (err) reject(err);
        else resolve(row.total);
      });
    });
  }
}

module.exports = Post;
```

---

### swagger.json（API 文档）

```json
{
  "openapi": "3.0.0",
  "info": {
    "title": "Blog API",
    "version": "1.0.0",
    "description": "博客系统 RESTful API"
  },
  "servers": [
    {
      "url": "http://localhost:3000/api",
      "description": "本地开发服务器"
    }
  ],
  "components": {
    "securitySchemes": {
      "bearerAuth": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "JWT"
      }
    }
  },
  "security": [
    {
      "bearerAuth": []
    }
  ],
  "paths": {
    "/posts": {
      "get": {
        "summary": "获取所有文章",
        "parameters": [
          {
            "name": "page",
            "in": "query",
            "schema": { "type": "integer", "default": 1 }
          },
          {
            "name": "limit",
            "in": "query",
            "schema": { "type": "integer", "default": 10 }
          },
          {
            "name": "category",
            "in": "query",
            "schema": { "type": "integer" }
          },
          {
            "name": "status",
            "in": "query",
            "schema": { "type": "string", "enum": ["draft", "published"] }
          }
        ],
        "responses": {
          "200": {
            "description": "成功获取文章列表"
          }
        }
      },
      "post": {
        "summary": "创建新文章",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["title", "content"],
                "properties": {
                  "title": { "type": "string" },
                  "content": { "type": "string" },
                  "category_id": { "type": "integer" },
                  "status": { "type": "string", "enum": ["draft", "published"] }
                }
              }
            }
          }
        },
        "responses": {
          "201": { "description": "文章创建成功" },
          "400": { "description": "参数错误" }
        }
      }
    }
  }
}
```

---

## 测试 API

### 使用 curl 测试

```bash
# 1. 注册用户
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","password":"123456","email":"test@example.com"}'

# 2. 登录获取 token
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","password":"123456"}'

# 3. 创建文章（需要 token）
curl -X POST http://localhost:3000/api/posts \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -d '{"title":"测试文章","content":"这是内容","status":"published"}'

# 4. 获取文章列表
curl http://localhost:3000/api/posts?page=1&limit=10
```

---

## 关键要点

1. **用 Claude Code 生成骨架**：5 分钟生成生产级 API 代码
2. **参数校验不可少**：用 `express-validator` 避免脏数据
3. **错误处理要完整**：全局错误中间件统一处理
4. **JWT 认证保护路由**：敏感操作必须登录
5. **生成 API 文档**：Swagger 让前端对接更轻松

---

## 下一步

- 2.3 节：前端组件生成（用 Claude Code 写 React/Vue 组件）
- 2.4 节：配置文件管理（环境变量、多环境配置）

---

**💡 提示：** 如果团队有 API 规范（如 Google API Style Guide），可以告诉 Claude Code，让它按照规范生成代码和文档。
